Show what the SmartEntry Pro self-learning engine has learned. Fetch http://localhost:3001/api/learning

Report:

SELF-LEARNING REPORT
Sessions run: X | Last updated: [date]

SETUP PERFORMANCE (learned from real trades):
┌─────────────────────┬──────┬──────┬────────┬────────┬──────────────┐
│ Setup               │ Wins │ Loss │ WR %   │ P&L    │ Boost        │
├─────────────────────┼──────┼──────┼────────┼────────┼──────────────┤
│ [each setup]        │  X   │  X   │  X%    │ $X     │ [+X BOOST /  │
│                     │      │      │        │        │  -X PENALTY] │
└─────────────────────┴──────┴──────┴────────┴────────┴──────────────┘

WHAT THE SYSTEM LEARNED:
• [list any setups with boost/penalty — explain in plain English what this means for signal quality]

SETUPS STILL LEARNING (< 5 trades): [list them]
SETUPS TO CONSIDER DISABLING (WR < 40%): [list them or "none"]

Then ask: "Want to reset learning, disable a weak setup, or see the full journal?"
