# DAILY-2026-08-19 — recommendations

No TaskCreate tool exists in this harness, so these are persisted here rather than
reported as a task count that would not survive the session.

## REC 1 — HIGH — Report expectancy in R, not dollars
WHAT:   get_performance pools dollar P&L across assets whose per-trade risk differs 24x.
WHY:    SPX 0.1 lot = ~$2 risk, BTC 0.01 lot = ~$7, GOLD 0.01 lot = ~$47. valuePerPoint
        per lot is $1 / $1 / $100. So expectancy -104.89 and avg_loss -185.15 describe
        Gold's contract size, not edge. avgRealizedR is already -1 for all three setups.
HOW:    server/index.js — the /api/performance setups block already computes
        totalRealizedR and avgRealizedR. Promote R to the headline field and mark the
        dollar aggregates as non-comparable across assets.
IMPACT: Removes a systematically misleading number. No trade behaviour change.
EFFORT: Simple (<30min)

## REC 2 — HIGH — fixedLotSize is inert for SPX; say so where it is set
WHAT:   fixedLotSize 0.01 cannot apply to SP500 because the broker volume_min is 0.1.
WHY:    mt5_bridge.py get_lot_size ends with lots = max(lots, sym_info.volume_min),
        so SPX always trades 0.1 however low fixedLotSize goes. Risk-based sizing
        wanted 6.46 lots; the clamp did its job, but the floor is the broker's.
        Per CLAUDE.md: a setting with no reader is decoration.
HOW:    Surface the effective floor per symbol on the strategy-settings surface, so
        the number shown is what will actually trade.
IMPACT: Prevents a future sizing change from silently not applying to SPX.
EFFORT: Medium (30-90min)

## REC 3 — MEDIUM — BTC's live cohort is arithmetically dead
WHAT:   BTC's current setup is H4-only MODERATE at conf 50; that cohort's ceiling is
        65 against gate 70, so it can never fire.
WHY:    cohort_reachability.cjs lists it as DEAD, 5 short. BTC has not traded in 7.5
        days and cannot while this is its live cohort.
HOW:    Decide it explicitly per the tool's own advice — block it by name, or bring
        its base within the boost stack. Do NOT move the gate; 70 is defended by its
        best-worst-fold and is coupled to the Gold squeeze cohort.
IMPACT: Either restores BTC as a tradeable asset or makes its silence honest.
EFFORT: Complex (>90min) — needs cohort_walkforward evidence first.

## REC 4 — MEDIUM — Exa MCP is down
WHAT:   web_search_exa returns 401 Invalid API key.
WHY:    Confirmed live this session. Research steps silently fall back to Brave only.
HOW:    Rotate the Exa key in keys.env (never commit it).
IMPACT: Restores the second research provider.
EFFORT: Simple (<30min)
