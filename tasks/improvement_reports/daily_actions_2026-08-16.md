# DAILY ACTIONS — 2026-08-16 (07:15 local / 06:15 UTC)

Generated by /daily. HIGH and CRITICAL items only. Each carries the condition that retires it.

---

## [CRITICAL] Fleet holds XAUUSD BUY on BOTH boxes — exposure on either screen is a fraction of the real number

**WHAT:** The laptop and the VPS each hold an open XAUUSD BUY. `tasks/doctor.cjs` reports
3 positions across the fleet. Each dashboard shows only its own box, so neither screen
states the fleet's real gold concentration, and the legs move together.

**WHY (data):**
- doctor 2026-08-16T06:18:30Z: `fleet: 1 position(s) held on BOTH boxes at once — XAUUSD BUY on this box + peer — 3 positions across the fleet`
- Local leg: ticket 1742893638, XAUUSD BUY 0.01 @ 4414.05, SL 4276.77 → ~137.28 risk units per leg.
- Both boxes independently report `consecutiveLosses: 2` of a 3-loss breaker. Neither
  breaker knows about the other's losses, so the fleet can absorb 4 losses before either trips.
  This is the mechanism already recorded in `account_11581419_traded_by_two_machines`.

**HOW:** Decide whether the DUPLICATE gate should be fleet-aware rather than per-box.
Today `DUPLICATE` is evaluated against the local position list only (gate-health shows
`DUPLICATE killed:2` on this box — it correctly blocked the local re-entry, and had no
knowledge of the peer's leg). Do NOT close anything — the standing constraint is that an
open trade is never closed; the sample is scarcer than the money.

**RETIRES WHEN:** either the duplicate guard consults the peer's position list, or the
fleet-level concentration is deliberately accepted and written into standing notes.

**EFFORT:** Medium (30–90min) for the guard; Simple to accept-and-document.

---

## [HIGH] 182 rejection rows are permanently unscorable — and they are 53% of a doubled sample

**WHAT:** The rejection ledger is the only instrument that manufactures trade evidence at
zero risk, and 182 of its 649 rows can never be scored.

**WHY (data):**
- `tasks/rejections.jsonl`: 649 rows, 182 with no `sourceSymbol`.
- Every one of those 182 carries the timestamp `2026-08-09T07:45` — a single backfill batch,
  not an ongoing leak.
- The writer is already fixed: every row before (from 2026-08-07T04:09) and after
  (through 2026-08-16T06:14) has `sourceSymbol` populated.
- Currently resolved across the whole ledger: 159. Recovering 182 would more than double it.
- Confirms and dates the existing note `rejection_ledger_lost_182_rows_at_source`.

**HOW:** Re-run the 2026-08-09 replay through `rejection_log.js` with the current writer,
then `tasks/score_rr_rejections.py` → `tasks/learning_from_rejections.py`. Read
`tasks/REJECTION-LEDGER-SPEC.md` first. Re-scoring alone cannot fix it — the symbol was
never written, so the source replay has to be regenerated.

**RETIRES WHEN:** `/api/rejection-evidence` `totals.resolved` exceeds ~300 and the
unscorable count for MIN_RR falls well below 227.

**EFFORT:** Medium (30–90min).

---

## [HIGH] Two market-hour jobs report RED on the VPS while both are GREEN on the laptop

**WHAT:** The peer's coverage audit reports RED for `SmartEntry Post-Close Analysis` and
`SmartEntry Pre-Open Plan`. This box's own coverage audit is 22/22 green, so the failure is
confined to the box that trades continuously.

**WHY (data):**
- doctor 2026-08-16T06:18:30Z: `peer: job Coverage Audit reports RED — coverage is RED:
  SmartEntry Post-Close Analysis, SmartEntry Pre-Open Plan, peer THEMIS — reported 1h ago`
- Local `tasks/logs/coverage_audit.txt` at 07:15:04: both jobs GREEN
  (Post-Close ok 23.7h ago, Pre-Open ok 11.3h ago), 0 RED across 22 checks.
- These are the two deterministic market-hour jobs added because everything previously ran
  inside ASIAN — the one session that measured negative (`jobs_were_scheduled_against_the_wall_clock`).
- The `peer THEMIS` component of that RED is explained and self-clearing: this laptop was
  down from 2026-08-15 20:54 to 2026-08-16 07:12, so no heartbeat was sent overnight.
  The two job REDs are NOT explained by that.

**HOW:** Read the VPS's own `tasks\logs\coverage_audit.txt`. Use `ssh -EncodedCommand`
(a nested-quote ssh silently returns LOCAL results — `ssh_to_vps_needs_encodedcommand`).
Cause not yet established: it is either a missing scheduled task on the VPS or the jobs
failing there. Do not guess which before reading the log.

**RETIRES WHEN:** the peer's coverage audit returns GREEN for both jobs.

**EFFORT:** Simple to diagnose (<30min); fix depends on cause.

---

## [HIGH] The Exa research provider is dead — every research step runs at half coverage, silently

**WHAT:** `mcp__exa__web_search_exa` returns `401 Invalid API key`.

**WHY (data):** Called 2026-08-16T06:18:17Z during this run's Step 5 →
`web_search_exa error (401): Invalid API key`. Exa is documented as the second provider,
"for research the Brave index misses". Brave answered normally, so the daily research step
completed looking successful while covering one index instead of two.

**HOW:** Rotate the Exa key where the MCP server reads it. Do not write the key into any
note or doc.

**RETIRES WHEN:** an `web_search_exa` call returns results.

**EFFORT:** Simple (<30min).

---

## Deliberately NOT recommended today

- **Do not touch MIN_RR.** Its verdict flipped to COSTING MONEY (121 resolved, 71%
  would-have-won, +14.47R), but that aggregate is cancellation between
  RANGE_TRADE_LONG (55W/12L) and BUY_OVERSOLD (25W/0L) on one side and
  RANGE_TRADE_SHORT (6W/23L, −21.3R) on the other. The 4-year sweep says lowering it costs
  6.6R, and where the ledger contradicts a walk-forward the walk-forward wins. The only
  measurable next step is a per-setup MIN_RR walk-forward WITH COSTS.
- **Do not veto RANGE_TRADE_SHORT in RANGING.** Already measured and failed its own
  year-split test (`range_trade_short_ranging_veto_not_supported`).
- **Do not act on BUY_OVERSOLD's 25W/0L.** Its walk-forward is −0.332R/trade
  (`buy_oversold_ledger_contradicts_walkforward`).
- **Do not "fix" the 4 unreachable cohorts** logged at startup. Dead does not imply
  wrongly dead (`spx_h4_only_is_correctly_blocked`).

## LOW / noted, no action

- Pre-open trigger was set before the current plan was built — healable:
  `powershell -File tasks\reschedule_preopen.ps1`.
- Gold basis: `/api/status` shows 4437.30 (Yahoo GC=F) while signals use 4375.61
  (MT5 XAUUSD) — $61.69 apart, up from the $51 previously recorded. Known and expected;
  the header and the signal are quoting different instruments.
- `startup_all` at 07:15:07 warned of an untagged bridge (PID 28780). Both that PID and the
  tagged PID 17228 were gone by 07:16:03; `?account=default` reports never-connected, and
  the two current python PIDs both started 07:16:03 (the normal shim pair). No untagged
  bridge is running now.
- 680 `Polling: Request failed with status code 409` lines in server_log all predate the
  current server generation; the Telegram webhook now disables polling at startup. Historical.
