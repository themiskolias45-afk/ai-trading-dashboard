# Engineer Plan — 2026-07-25T15:19:01.674Z

Task: Add automated test scripts for the SmartEntry Pro server. Workstream A: write a test script for server/sizing.js (Kelly-based position sizing / validateTrade logic) covering normal trades, edge cases (zero account balance, stop price equal to entry price, missing fields), and invalid input, runnable via node. Workstream B: write a test script for server/hermes.js (self-learning boost/penalty calibration logic) covering a setup with high win rate (boosted), low win rate (penalised), and too few trades (still learning), runnable via node. Both are new standalone test files only � do not modify sizing.js, hermes.js, or any other existing file.

## 1. test-sizing
Files: server/sizing.test.js (new file, standalone)

Write a new standalone Node.js test script at server/sizing.test.js that tests the position-sizing and trade-validation logic in server/sizing.js. Do NOT modify server/sizing.js or any other existing file - create only this one new test file. First, open server/sizing.js and inspect its module.exports to learn the exact names and signatures of the exported functions (likely a Kelly-based position sizing function and a validateTrade function). Import them via require('./sizing.js') using the actual exported names you find. Write a self-contained test runner that does NOT depend on any external test framework (no jest/mocha) - implement a tiny helper like `function assert(cond, msg){ if(!cond){ console.error('FAIL: '+msg); failures++; } else { console.log('PASS: '+msg); } }` and track a failures counter, then at the end call process.exit(failures ? 1 : 0). Cover these cases: (1) Normal valid trades - realistic account balance, entry price, stop price, win probability, and payoff ratio; assert the computed position size / Kelly fraction is a positive finite number within sensible bounds. (2) Edge case: zero account balance - assert it returns zero size or a handled result rather than NaN/throwing unexpectedly. (3) Edge case: stop price equal to entry price (zero risk per unit / division-by-zero risk) - assert it is handled gracefully (no Infinity/NaN leaking through, or a clear rejection). (4) Missing required fields (undefined/null entry, stop, balance, or probability) - assert validateTrade rejects or flags them. (5) Invalid input types (negative balance, probability >1 or <0, non-numeric strings) - assert they are rejected. Match your assertions to the ACTUAL return shape of the functions (inspect what they return - object, number, {valid, reason}, etc.) rather than assuming. The script must be runnable directly via `node server/sizing.test.js` and must print a clear summary line (e.g. total/passed/failed) at the end.

## 2. test-hermes
Files: server/hermes.test.js (new file, standalone)

Write a new standalone Node.js test script at server/hermes.test.js that tests the self-learning boost/penalty calibration logic in server/hermes.js. Do NOT modify server/hermes.js or any other existing file - create only this one new test file. First, open server/hermes.js and inspect its module.exports to learn the exact names and signatures of the exported functions and any config/threshold constants (the calibration logic that boosts a trading setup when its historical win rate is high, penalises it when the win rate is low, and returns a neutral/still-learning result when there are too few trades to judge). Import them via require('./hermes.js') using the actual exported names you find. Note the minimum-trades threshold and win-rate thresholds the code actually uses so your test inputs land clearly on the correct side of each boundary. Write a self-contained test runner that does NOT depend on any external test framework (no jest/mocha) - implement a tiny helper like `function assert(cond, msg){ if(!cond){ console.error('FAIL: '+msg); failures++; } else { console.log('PASS: '+msg); } }` and track a failures counter, then at the end call process.exit(failures ? 1 : 0). Cover these cases: (1) High win rate with sufficient trade count - assert the setup receives a boost (multiplier >1 / positive adjustment / whatever the actual return shape indicates). (2) Low win rate with sufficient trade count - assert the setup is penalised (multiplier <1 / negative adjustment). (3) Too few trades (below the minimum sample size) - assert the result is neutral / 'still learning' (no boost or penalty applied, matching the actual neutral value the code returns). Match your assertions to the ACTUAL return shape and threshold values found in hermes.js rather than assuming. The script must be runnable directly via `node server/hermes.test.js` and must print a clear summary line (e.g. total/passed/failed) at the end.
