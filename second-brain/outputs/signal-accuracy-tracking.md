Currently tracking executed trades in the trade journal but NOT tracking every signal that fires — only ones that execute. Need to add signal accuracy log:
- Log every signal with confidence score, asset, direction, timeframe alignment
- Track whether signal reached target, hit stop, or expired
- Compare AI-approved vs non-approved signal outcomes
- Per-asset win rate breakdown
- Track false positives (high confidence signals that lost)
This would help tune the 65% confidence threshold and identify which setups actually work.
